#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(git rev-parse --show-toplevel)"
cd "$ROOT_DIR"

echo "[mirofish] pre-commit: backend lite tests + frontend tests"
bash ./scripts/validate_repo.sh --skip-frontend-build
